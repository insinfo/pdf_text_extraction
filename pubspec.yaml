name: pdf_text_extraction
description: pdf_text_extraction
version: 1.0.0
homepage: https://github.com/insinfo/pdf_text_extraction
#repository: https://github.com/insinfo/pdf_text_extraction

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  ffi: ^1.1.2
  path: ^1.8.1 
  
dev_dependencies: 
  build_runner: ^2.1.2
  ffigen: ^2.4.2
  #for test only
  http: ^0.13.3
  postgres: ^2.4.3
  intl: ^0.17.0
  html: ^0.15.0
#dart run ffigen --config ffigen.yaml